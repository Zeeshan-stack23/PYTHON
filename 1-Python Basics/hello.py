if False:
    print("Correct Indentation")  # This will execute because the condition is True
    if True:
        print("This won't print")  # This will NOT execute because the condition is False
    print("This will print")       # This will execute because it's outside the inner if block
    print("Outside the if block")      # This will execute because it's outside all if blocks